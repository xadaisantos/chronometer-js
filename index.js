let chronometer = new Chronometer();

let alternateColors = () => {
    let alvo = document.querySelector("#content");
    alvo.classList.toggle("dark-mode");
    alvo.classList.toggle("light-mode");
}
